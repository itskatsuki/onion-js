module.exports = {
    calculator: function (num1, operatior, num2) {
        if(!num1) throw new TypeError(`Missing number 1 :(`)
        if(!operator) throw new TypeError(`Missing Second number :(`)
    }
}